# ─── Core API ─────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pillow>=10.0
numpy>=1.24
opencv-python>=4.8
matplotlib>=3.7
requests>=2.31          # io1/io2/io4/io6 — kept for the chatbot module (not used by the verdicts)

# ─── ML core (torch ecosystem) ────────────────────────────────────────────
torch>=2.0
torchvision>=0.15
transformers>=4.35      # TrOCR, RoBERTa, ViT, DETR, MiniLM…
accelerate>=0.25        # speed up HuggingFace loading
sentencepiece>=0.1.99   # tokenizers (translator, etc.)
sacremoses>=0.0.53      # opus-mt translation tokenisation

# ─── Vision / multimodal ──────────────────────────────────────────────────
open-clip-torch>=2.24   # io2 (zero-shot clickbait), io3 (coherence), io5 (caption fidelity)
ultralytics>=8.1        # YOLOv8 — io3, io6
easyocr>=1.7            # io2, io3, io5, io6
openai-whisper>=20231117  # io3, io6
segment-anything>=1.0   # io3 (optional)
facenet-pytorch>=2.5    # io1 — MTCNN face detection (matches Islem's ResNet50 training distribution)

# ─── Semantic / NLP ────────────────────────────────────────────────────────
sentence-transformers>=2.2  # MiniLM semantic match — io6

# ─── Knowledge-base loading (io6) ─────────────────────────────────────────
pandas>=2.0
openpyxl>=3.1
